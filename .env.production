# Origem do backend de produção (com SSL/HTTPS).
# NÃO precisa de /api no fim — o código (src/lib/api.ts) acrescenta /api automaticamente.
VITE_API_URL=https://api.yevatrade.com
VITE_APP_NAME=YevaTrade
VITE_APP_ENV=production
